# DraftDiff — landing page

Static marketing/landing page for DraftDiff (product URL for the Riot Developer Portal application).

Plain `index.html` + `style.css`, served by nginx via the included `Dockerfile`. No build step, no frameworks.

This repository contains the public landing page only — not the DraftDiff application or model code.
